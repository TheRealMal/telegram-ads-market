# API base URL (market service). No trailing slash.
NEXT_PUBLIC_API_URL=http://localhost:8080

# Optional: Telegram Web App init data for local auth. When running in a browser (not inside Telegram),
# the app has no init data, so auth fails. Paste the init data string here to auth locally.
# Get it from the Mini App URL fragment: #tgWebAppData=<value> — use the decoded value (same format
# as Telegram would send). Example format: user={"id":123,...}&auth_date=...&hash=...
# NEXT_PUBLIC_TG_WEB_APP_DATA=

# Username (with or without @) shown in "How to add a channel" instruction on profile.
# Example: therealmal or @therealmal
# NEXT_PUBLIC_ADD_CHANNEL_USERNAME=therealmal
